# Source-controlled release metadata for FLASH-Ghostty.
#
# GitHub releases, tags, release-workflow history, old development-session
# records, and artifacts available on this machine showed no prior signed or
# public distribution when this first candidate was selected. Choose each future
# release version deliberately and strictly increment FLASH_GHOSTTY_BUNDLE_VERSION
# for every new candidate, including any version later found outside GitHub.
FLASH_GHOSTTY_RELEASE_METADATA_SCHEMA=1
FLASH_GHOSTTY_RELEASE_VERSION=0.1.0
FLASH_GHOSTTY_BUNDLE_VERSION=1.0.0
